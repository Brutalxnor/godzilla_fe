export const GetUserById = async (id: string) => {
    try {
        const res = await 
    }
    catch (err) {
        console.error("Error fetching athlete subscriptions:", err);
        throw err;
      }
}